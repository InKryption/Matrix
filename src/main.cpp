#include "main.hpp"
#include <iostream>
#include "Matrix.hpp"

template<typename T, size_t sz> using arr = T[sz];
template<typename T> using ptr = T*;

int main(int argc, char* argv[])
{
	
	ink::Matrix mtrx;
	
	return 0;
}
